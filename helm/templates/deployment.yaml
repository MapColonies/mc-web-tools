{{- if .Values.client.enabled -}}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ template "mc-web-tools.fullname" . }}
  labels:
    app: {{ template "mc-web-tools.name" . }}
    component: mc-web-tools-deployment
    environment: {{ .Values.environment }}
    release: {{ .Release.Name }}
  annotations:
    collectord.io/index: {{ quote .Values.splunkIndex }}    
spec:
  replicas: {{ .Values.client.replicaCount }}
  selector:
    matchLabels:
      app: {{ template "mc-web-tools.name" . }}
      release: {{ .Release.Name }}
      run: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app: {{ template "mc-web-tools.name" . }}
        release: {{ .Release.Name }}
        run: {{ .Release.Name }}
    spec:
      imagePullSecrets:
        - name: {{ .Values.pullSecretName }}
      containers:    
        - name: {{ .Chart.Name }}
          image: "{{ .Values.client.image }}:{{ .Values.client.imageTag}}"
          imagePullPolicy: {{ .Values.imagePullPolicy }}
          envFrom:
            - configMapRef:
                name: {{ template "mc-web-tools.fullname" . }}-configmap
          ports:
            - name: http
              containerPort: 8080
              protocol: TCP
          livenessProbe:
            httpGet:
              path: /
              port: 8080
            initialDelaySeconds: 30
            timeoutSeconds: 1
          readinessProbe:
            httpGet:
              path: /
              port: 8080
          {{- if .Values.client.resources.enabled }}
          resources:
            requests:
              memory: {{ .Values.client.resources.requests.memory }}
              cpu: {{ .Values.client.resources.requests.cpu }}
            limits:
              memory: {{ .Values.client.resources.limits.memory }}
              cpu: {{ .Values.client.resources.limits.cpu }}
          {{- end }}
{{- end }}
